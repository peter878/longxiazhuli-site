---
import BaseLayout from '../../layouts/BaseLayout.astro';
---

<BaseLayout title="5 分钟快速安装 OpenClaw - 龙虾助理">
  <h1>📥 5 分钟快速安装 OpenClaw</h1>
  <p class="tag">新手入门 第 2 篇</p>

  <div class="tip">
    <p><strong>一句话总结：</strong>选择你的操作系统，跟着步骤走，10 分钟搞定！</p>
  </div>

  <h2>选择你的操作系统</h2>
  
  <div class="os-selector">
    <a href="#windows" class="os-btn">🪟 Windows</a>
    <a href="#macos" class="os-btn">🍎 macOS</a>
    <a href="#linux" class="os-btn">🐧 Linux</a>
  </div>

  <h2 id="windows">🪟 Windows 安装指南</h2>

  <h3>前置要求</h3>
  <ul>
    <li>Windows 10/11</li>
    <li>Node.js 18+ (<a href="https://nodejs.org/" target="_blank">下载地址</a>)</li>
    <li>PowerShell 或 CMD</li>
  </ul>

  <h3>步骤 1 - 安装 Node.js</h3>
  <ol>
    <li>访问 <a href="https://nodejs.org/" target="_blank">https://nodejs.org/</a></li>
    <li>下载 LTS 版本（推荐 20.x）</li>
    <li>运行安装程序，一路 Next</li>
    <li>验证安装：<pre><code>node --version
npm --version</code></pre></li>
  </ol>

  <h3>步骤 2 - 安装 OpenClaw</h3>
  <p>以管理员身份打开 PowerShell，运行：</p>
  <pre><code>npm install -g openclaw</code></pre>
  
  <p>或使用 pnpm（如果你有的话）：</p>
  <pre><code>pnpm add -g openclaw</code></pre>

  <h3>步骤 3 - 配置环境变量（如果需要）</h3>
  <p>如果提示"找不到 openclaw 命令"：</p>
  <ol>
    <li>打开"此电脑" → 属性 → 高级系统设置</li>
    <li>点击"环境变量"</li>
    <li>在"系统变量"中找到 Path，点击编辑</li>
    <li>新建，添加：<pre><code>C:\Users\你的用户名\AppData\Roaming\npm</code></pre></li>
    <li>确定保存，重新打开 PowerShell</li>
  </ol>

  <h3>步骤 4 - 初始化工作区</h3>
  <pre><code>mkdir my-assistant
cd my-assistant
openclaw init</code></pre>

  <h3>步骤 5 - 启动 OpenClaw</h3>
  <pre><code>openclaw start</code></pre>

  <hr/>

  <h2 id="macos">🍎 macOS 安装指南</h2>

  <h3>前置要求</h3>
  <ul>
    <li>macOS 10.15+</li>
    <li>Node.js 18+ (推荐用 Homebrew 安装)</li>
    <li>Terminal</li>
  </ul>

  <h3>步骤 1 - 安装 Homebrew（如果没有）</h3>
  <pre><code>/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"</code></pre>

  <h3>步骤 2 - 安装 Node.js</h3>
  <pre><code>brew install node</code></pre>
  
  <p>验证安装：</p>
  <pre><code>node --version
npm --version</code></pre>

  <h3>步骤 3 - 安装 OpenClaw</h3>
  <pre><code>npm install -g openclaw</code></pre>
  
  <p>或使用 pnpm：</p>
  <pre><code>pnpm add -g openclaw</code></pre>

  <h3>步骤 4 - 配置环境变量（如果需要）</h3>
  <p>如果提示"找不到 openclaw 命令"，添加到 shell 配置：</p>
  <pre><code># Intel Mac
export PATH="/usr/local/share/npm/bin:$PATH"

# Apple Silicon Mac
export PATH="/opt/homebrew/share/npm/bin:$PATH"</code></pre>
  
  <p>添加到 <code>~/.zshrc</code> 或 <code>~/.bash_profile</code>：</p>
  <pre><code>echo 'export PATH="/opt/homebrew/share/npm/bin:$PATH"' >> ~/.zshrc
source ~/.zshrc</code></pre>

  <h3>步骤 5 - 初始化工作区</h3>
  <pre><code>mkdir my-assistant
cd my-assistant
openclaw init</code></pre>

  <h3>步骤 6 - 启动 OpenClaw</h3>
  <pre><code>openclaw start</code></pre>

  <hr/>

  <h2 id="linux">🐧 Linux 安装指南</h2>

  <h3>前置要求</h3>
  <ul>
    <li>Ubuntu 20.04+ / Debian 11+ / CentOS 8+</li>
    <li>Node.js 18+</li>
    <li>sudo 权限</li>
  </ul>

  <h3>步骤 1 - 安装 Node.js</h3>
  
  <h4>Ubuntu/Debian:</h4>
  <pre><code># 使用 NodeSource 源
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs</code></pre>

  <h4>CentOS/RHEL:</h4>
  <pre><code>curl -fsSL https://rpm.nodesource.com/setup_20.x | sudo bash -
sudo yum install -y nodejs</code></pre>

  <h4>Arch Linux:</h4>
  <pre><code>sudo pacman -S nodejs npm</code></pre>

  <p>验证安装：</p>
  <pre><code>node --version
npm --version</code></pre>

  <h3>步骤 2 - 安装 OpenClaw</h3>
  <pre><code>sudo npm install -g openclaw</code></pre>
  
  <p>或使用 pnpm：</p>
  <pre><code>sudo pnpm add -g openclaw</code></pre>

  <h3>步骤 3 - 配置环境变量（如果需要）</h3>
  <p>添加到 <code>~/.bashrc</code> 或 <code>~/.zshrc</code>：</p>
  <pre><code>export PATH=$PATH:$(npm config get prefix)/bin</code></pre>
  
  <p>生效：</p>
  <pre><code>source ~/.bashrc</code></pre>

  <h3>步骤 4 - 初始化工作区</h3>
  <pre><code>mkdir my-assistant
cd my-assistant
openclaw init</code></pre>

  <h3>步骤 5 - 启动 OpenClaw</h3>
  <pre><code>openclaw start</code></pre>

  <hr/>

  <h2>配置 API Key（所有系统通用）</h2>

  <p>OpenClaw 需要配置大模型 API 才能工作。推荐使用阿里云百炼：</p>

  <ol>
    <li>访问阿里云百炼控制台 (<a href="https://bailian.console.aliyun.com/" target="_blank">链接</a>)</li>
    <li>注册/登录账号</li>
    <li>创建 API Key</li>
    <li>复制 Key 到配置文件</li>
  </ol>

  <h3>配置文件位置</h3>
  <ul>
    <li><strong>Windows:</strong> <code>%APPDATA%\openclaw\config.json</code></li>
    <li><strong>Mac/Linux:</strong> <code>~/.openclaw/config.json</code></li>
  </ul>
  
  <pre><code>{
  "defaultModel": "bailian/qwen3.5-plus",
  "apiKey": "你的 API_KEY"
}</code></pre>

  <h2>验证安装（所有系统通用）</h2>

  <pre><code>openclaw --version
openclaw status
openclaw ask "你好，请介绍一下自己"</code></pre>

  <h2>常见问题</h2>

  <h3>Q - 安装失败怎么办？</h3>
  <p>清理 npm 缓存后重试：</p>
  <pre><code>npm cache clean --force
npm install -g openclaw</code></pre>

  <h3>Q - 权限错误（Linux/Mac）？</h3>
  <p>使用 sudo 或修改 npm 目录权限：</p>
  <pre><code># Mac/Linux
sudo chown -R $(whoami) $(npm config get prefix)/{lib/node_modules,bin,share}</code></pre>

  <h3>Q - Windows Defender 报错？</h3>
  <p>可能是 PowerShell 执行策略限制：</p>
  <pre><code>Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser</code></pre>

  <h3>Q - API Key 无效？</h3>
  <ul>
    <li>检查 Key 是否正确复制（无多余空格）</li>
    <li>确认账号有足够额度</li>
    <li>检查模型名称是否正确</li>
  </ul>

  <div class="tip success">
    <h3>✅ 安装成功！</h3>
    <p>接下来学习 <a href="/guide/first-chat/">第一次和龙虾对话</a> 吧！</p>
  </div>

  <div style="margin-top: 2rem; text-align: center;">
    <a href="/guide/" class="btn">← 上一步：OpenClaw 是什么</a>
    <a href="/guide/first-chat/" class="btn" style="margin-left: 1rem;">下一步：第一次对话 →</a>
  </div>

  <style>
    .os-selector {
      display: flex;
      gap: 1rem;
      margin: 1.5rem 0;
      flex-wrap: wrap;
    }
    
    .os-btn {
      display: inline-block;
      padding: 0.75rem 1.5rem;
      background: #f3f4f6;
      border: 2px solid #e5e7eb;
      border-radius: 8px;
      text-decoration: none;
      color: #1f2937;
      font-weight: 500;
      transition: all 0.2s;
    }
    
    .os-btn:hover {
      background: #dc2626;
      border-color: #dc2626;
      color: white;
      transform: translateY(-2px);
    }
    
    hr {
      margin: 3rem 0;
      border: none;
      border-top: 2px solid #e5e7eb;
    }
    
    h2 {
      margin-top: 0;
    }
    
    pre {
      background: #1f2937;
      color: #f3f4f6;
      padding: 1rem;
      border-radius: 6px;
      overflow-x: auto;
    }
    
    code {
      font-family: 'Consolas', 'Monaco', monospace;
    }
  </style>
</BaseLayout>

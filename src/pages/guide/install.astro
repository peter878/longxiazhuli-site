---
import BaseLayout from '../../layouts/BaseLayout.astro';
const installCmd = `npm install -g openclaw`;
const initCmd = `openclaw init`;
const statusCmd = `openclaw status`;
---

<BaseLayout title="5 分钟快速安装 OpenClaw - 龙虾助理">
  
  <div class="tip">
    <p><strong>一句话总结：</strong>只需 3 条命令，5 分钟内让你的 AI 龙虾上线 🦞</p>
  </div>

  <h1>🦞 5 分钟快速安装 OpenClaw</h1>
  <p class="tag">新手入门 第 0 篇</p>

  <div style="background: #f3f4f6; padding: 1.5rem; border-radius: 0.5rem; margin-bottom: 2rem;">
    <h3 style="margin-top: 0;">⚡ 快速安装命令</h3>
    <pre style="background: #1f2937; color: #f3f4f6; padding: 1rem; border-radius: 0.5rem; overflow-x: auto;"><code>{installCmd}</code></pre>
    <p style="margin: 0.5rem 0 0 0; color: #6b7280; font-size: 0.875rem;">安装完成后运行：<code>{initCmd}</code></p>
  </div>

  <h2>📋 系统要求</h2>
  
  <p>在开始之前，请确保你的系统满足以下要求：</p>

  <table style="width: 100%; border-collapse: collapse; margin: 1rem 0;">
    <thead>
      <tr style="background: #f3f4f6;">
        <th style="padding: 0.75rem; text-align: left; border: 1px solid #e5e7eb;">组件</th>
        <th style="padding: 0.75rem; text-align: left; border: 1px solid #e5e7eb;">要求</th>
        <th style="padding: 0.75rem; text-align: left; border: 1px solid #e5e7eb;">验证命令</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td style="padding: 0.75rem; border: 1px solid #e5e7eb;">Node.js</td>
        <td style="padding: 0.75rem; border: 1px solid #e5e7eb;">v18.0 或更高</td>
        <td style="padding: 0.75rem; border: 1px solid #e5e7eb;"><code>node -v</code></td>
      </tr>
      <tr>
        <td style="padding: 0.75rem; border: 1px solid #e5e7eb;">npm</td>
        <td style="padding: 0.75rem; border: 1px solid #e5e7eb;">v8.0 或更高</td>
        <td style="padding: 0.75rem; border: 1px solid #e5e7eb;"><code>npm -v</code></td>
      </tr>
      <tr>
        <td style="padding: 0.75rem; border: 1px solid #e5e7eb;">操作系统</td>
        <td style="padding: 0.75rem; border: 1px solid #e5e7eb;">Windows / macOS / Linux</td>
        <td style="padding: 0.75rem; border: 1px solid #e5e7eb;">-</td>
      </tr>
      <tr>
        <td style="padding: 0.75rem; border: 1px solid #e5e7eb;">磁盘空间</td>
        <td style="padding: 0.75rem; border: 1px solid #e5e7eb;">至少 500MB</td>
        <td style="padding: 0.75rem; border: 1px solid #e5e7eb;">-</td>
      </tr>
    </tbody>
  </table>

  <div class="tip" style="background: #fef3c7;">
    <h3 style="margin-top: 0; color: #92400e;">💡 没有 Node.js？</h3>
    <p style="margin: 0;">前往 <a href="https://nodejs.org/" target="_blank">nodejs.org</a> 下载并安装 LTS 版本。安装完成后重启终端。</p>
  </div>

  <h2>🚀 安装步骤</h2>

  <h3>步骤 1：打开终端</h3>
  
  <ul>
    <li><strong>Windows:</strong> 按 <code>Win + R</code>，输入 <code>powershell</code>，回车</li>
    <li><strong>macOS:</strong> 按 <code>Cmd + Space</code>，输入 <code>Terminal</code>，回车</li>
    <li><strong>Linux:</strong> 按 <code>Ctrl + Alt + T</code> 或使用你喜欢的终端</li>
  </ul>

  <h3>步骤 2：安装 OpenClaw</h3>
  
  <p>在终端中运行以下命令：</p>
  <pre style="background: #1f2937; color: #f3f4f6; padding: 1rem; border-radius: 0.5rem; overflow-x: auto;"><code>{installCmd}</code></pre>
  
  <p>等待安装完成，你会看到类似输出：</p>
  <pre style="background: #f3f4f6; color: #1f2937; padding: 1rem; border-radius: 0.5rem; overflow-x: auto;"><code>added 125 packages in 30s

15 packages are looking for funding
  run `npm fund` for details</code></pre>

  <h3>步骤 3：验证安装</h3>
  
  <p>运行以下命令检查 OpenClaw 是否安装成功：</p>
  <pre style="background: #1f2937; color: #f3f4f6; padding: 1rem; border-radius: 0.5rem; overflow-x: auto;"><code>{statusCmd}</code></pre>
  
  <p>如果看到版本信息和状态，说明安装成功！🎉</p>

  <h3>步骤 4：初始化工作区</h3>
  
  <p>选择一个你喜欢的目录作为工作区，然后运行：</p>
  <pre style="background: #1f2937; color: #f3f4f6; padding: 1rem; border-radius: 0.5rem; overflow-x: auto;"><code>{initCmd}</code></pre>
  
  <p>按照提示完成初始化，OpenClaw 会创建以下结构：</p>
  <pre style="background: #f3f4f6; color: #1f2937; padding: 1rem; border-radius: 0.5rem; overflow-x: auto;"><code>workspace/
├── MEMORY.md          # 长期记忆
├── SOUL.md            # 人格设定
├── USER.md            # 用户信息
├── IDENTITY.md        # 身份定义
├── AGENTS.md          # 使用指南
├── TOOLS.md           # 工具配置
├── HEARTBEAT.md       # 心跳任务
└── memory/            # 日常记忆目录</code></pre>

  <h2>⚙️ 配置连接方式（可选）</h2>

  <p>OpenClaw 支持多种消息平台，你可以选择如何与你的龙虾交流：</p>

  <h3>方式 1：Web 控制台（默认）</h3>
  <p>无需额外配置，直接使用 Web 界面与 OpenClaw 对话。</p>

  <h3>方式 2：飞书（Feishu）</h3>
  <ol>
    <li>在飞书开放平台创建应用</li>
    <li>获取 App ID 和 App Secret</li>
    <li>在 OpenClaw 配置文件中添加飞书凭证</li>
  </ol>

  <h3>方式 3：Telegram</h3>
  <ol>
    <li>联系 @BotFather 创建机器人</li>
    <li>获取 Bot Token</li>
    <li>在 OpenClaw 配置文件中添加 Token</li>
  </ol>

  <h3>方式 4：Discord</h3>
  <ol>
    <li>在 Discord 开发者门户创建应用</li>
    <li>获取 Bot Token</li>
    <li>邀请机器人到你的服务器</li>
  </ol>

  <div class="tip" style="background: #e0e7ff;">
    <h3 style="margin-top: 0; color: #3730a3;">📚 详细配置文档</h3>
    <p style="margin: 0;">更多平台配置详情请查看 <a href="/advanced/config/">进阶配置指南</a>。</p>
  </div>

  <h2>❓ 常见问题</h2>

  <h3>Q: 安装时提示权限错误</h3>
  <p><strong>A:</strong> 在 Windows 上以管理员身份运行 PowerShell，或在 macOS/Linux 上使用 <code>sudo</code>：</p>
  <pre style="background: #1f2937; color: #f3f4f6; padding: 1rem; border-radius: 0.5rem; overflow-x: auto;"><code>sudo npm install -g openclaw</code></pre>

  <h3>Q: npm 安装很慢</h3>
  <p><strong>A:</strong> 可以切换到国内镜像源：</p>
  <pre style="background: #1f2937; color: #f3f4f6; padding: 1rem; border-radius: 0.5rem; overflow-x: auto;"><code>npm config set registry https://registry.npmmirror.com</code></pre>

  <h3>Q: 安装后找不到 openclaw 命令</h3>
  <p><strong>A:</strong> 需要重启终端，或者检查 npm 全局安装目录是否在 PATH 中。</p>

  <h3>Q: 可以在服务器上运行吗？</h3>
  <p><strong>A:</strong> 可以！OpenClaw 支持 Linux 服务器，推荐用 Docker 部署。</p>

  <h2>🎯 下一步</h2>

  <div style="display: flex; gap: 1rem; flex-wrap: wrap; margin-top: 1rem;">
    <a href="/guide/first-chat/" class="btn" style="flex: 1; min-width: 200px; text-align: center;">💬 第一次对话 →</a>
    <a href="/guide/workspace/" class="btn" style="flex: 1; min-width: 200px; text-align: center; background: #6b7280;">📂 工作区结构 →</a>
  </div>

  <div class="tip" style="margin-top: 2rem; background: #f0fdf4;">
    <h3 style="margin-top: 0; color: #166534;">✅ 安装完成清单</h3>
    <ul style="margin: 0;">
      <li>☑️ Node.js 已安装（v18+）</li>
      <li>☑️ OpenClaw 已安装（运行 <code>openclaw status</code> 验证）</li>
      <li>☑️ 工作区已初始化</li>
      <li>☑️ 准备好开始养龙虾了！🦞</li>
    </ul>
  </div>
</BaseLayout>
